---
title: "Instance Status"
description: "Retired 2026-08-13 — live checks moved into the Core Builds Preflight tool. Tables below are the final snapshot."
---

## Which Instance Should I Use?

The short answer: **ElfHosted** for most people, **ElfHosted Private** if you hit rate limits, **Yeb's** as a solid backup.

<CardGroup cols={3}>
  <Card title="ElfHosted (Free)" icon="star">
    Best starting point. Debrid-only — no P2P or HTTP streams, which matches Core Builds perfectly. All templates are tested and verified here first. Expect rate limiting under heavy use.
  </Card>
  <Card title="ElfHosted Private" icon="lock">
    Same host, no rate limits. ~$9/month from the ElfHosted store. Worth it if you stream several hours a day or share a config across multiple profiles.
  </Card>
  <Card title="Yeb's (fortheweak)" icon="shield">
    Run by an AIOStreams Discord admin. Fast and reliable. Uses a stricter regex whitelist — all Core Builds templates (v2.9.0+) are verified against it. Good free alternative to ElfHosted.
  </Card>
</CardGroup>

<Tip>
  **Self-hosting** is always the best option if you're comfortable with Docker — no rate limits, full control, and you stay on your own hardware. See the [AIOStreams self-hosting guide](https://github.com/Viren070/AIOStreams#self-hosting).
</Tip>

---

<Warning>
  **Green = host is reachable.** Does not guarantee the instance accepts new configs or that your template will import correctly. Always test with a fresh import after choosing a host.
</Warning>

## Stable Instances

{/* STATUS_STABLE_START */}
<Warning>
  **Live table retired (2026-08-13).** The every-6-hours auto-refresh was removed: it lived on a
  fragile write-to-main cron and its last weeks disagreed with reality (hosts shown online that were
  unreachable). For a live answer now, use **Core Builds → Tools → Preflight** — it probes the
  exact host you target, against your real config, on demand. Instance guidance below stays —
  it's timeless advice, not a heartbeat.
</Warning>
{/* STATUS_STABLE_END */}

---

## Nightly Instances

<Note>
  Nightly instances run the latest AIOStreams development build. Features may be incomplete or unstable. Use for Labs templates only.
</Note>

{/* STATUS_NIGHTLY_START */}
<Note>
  Nightly-instance health is likewise retired here. Nightly instances move fast by design; check
  your nightly host directly, or run its URL through Preflight before installing Labs builds.
</Note>
{/* STATUS_NIGHTLY_END */}

---

## Instance Notes

<AccordionGroup>
  <Accordion title="ElfHosted">
    Most stable public option. Public instance is debrid-only — no P2P or HTTP streams, which suits Core Builds perfectly. Paid private plan available at store.elfhosted.com/product/aiostreams (~$9/month) with no rate limits.
  </Accordion>
  <Accordion title="Yeb's (fortheweak)">
    Run by an AIOStreams Discord admin. Reliable uptime, fast. Uses a stricter regex whitelist than ElfHosted — as of v2.9.0 all Core Builds templates are verified against it.
  </Accordion>
  <Accordion title="Midnight's">
    Run by the TorBox community manager. Optimised for TorBox setups. ⚠️ Currently running **Meteor Rewrite V2 Beta** — Your Media is not yet available. You may need to reconfigure your manifest after importing.
  </Accordion>
  <Accordion title="Viren's Nightly">
    The AIOStreams developer's own instance. Nightly only — tracks the main branch directly. Most up-to-date features but least stable.
  </Accordion>
  <Accordion title="Kuu's">
    Community-run. Both stable and nightly available.
  </Accordion>
  <Accordion title="ATBP">
    Community-run. Good uptime track record.
  </Accordion>
  <Accordion title="Omni's">
    Community-run.
  </Accordion>
</AccordionGroup>

---

## Missing an Instance?

If you run a public AIOStreams instance and want it listed here, [open an issue](https://github.com/brevityA/Core-Builds/issues/new?title=Add+Instance:+&labels=instance) with the URL and it will be added to the status checker.
