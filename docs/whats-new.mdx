---
title: "What's New — Configurator v2.91"
sidebarTitle: "What's New"
description: "Latest Core Builds Configurator releases (v2.91 and earlier), auto-generated from the in-app changelog."
---

# What's New in the Configurator

This page is regenerated from `configurator/src/data/changelog.js` by `scripts/sync-docs.py`, so it always reflects the latest shipped configurator version (currently **v2.91**).

For the template-suite release log, see the [Changelog](https://corebuilds-docs.docsalot.dev/changelog).

## Configurator v2.91 (Aug 9 2026)

<CardGroup cols={2}>
  <Card title="Fixed: 'Rebuild loop'" icon="sparkles">
    the version banner's Rebuild button wiped all settings but never stamped the current version, so the outdated banner returned after every rebuild. Now keeps settings, stamps the version, and toasts the user to download + re-import.
  </Card>
  <Card title="Fixed: explicit AIOStreams host selection is now honored" icon="bolt">
    picking a specific host (e.g. Omni, ForTheWeak) no longer silently fails over to a different host. Outdated hosts (&lt; v2.32.0) get a clear version-gate error with remediation steps.
  </Card>
  <Card title="Express Install now shows a visible AIOStreams host dropdown (prefilled from Advanced), so the host choice is explicit from the start." icon="shield-check">
    Express Install now shows a visible AIOStreams host dropdown (prefilled from Advanced), so the host choice is explicit from the start.
  </Card>
</CardGroup>

---

## Configurator v2.90 (Aug 6 2026)

<CardGroup cols={2}>
  <Card title="AIOStreams v2.32 compatibility" icon="sparkles">
    the legacy built-in torbox-search preset is removed from all generated configs and all 72 static templates (the TorBox Search API was shut down; saving a config that still includes it fails on v2.32+ hosts). No automatic conversion to the generic Newznab option is performed.
  </Card>
  <Card title="Templates/Legacy/v2.31.1/" icon="bolt">
    explicit compatibility lane preserving the 15 previously-enabled templates with torbox-search for hosts still on AIOStreams 2.31.1 or older.
  </Card>
  <Card title="Minimum AIOStreams version raised to 2.32.0" icon="shield-check">
    host auto-select and version floor now match the current public host fleet (all stable instances are v2.32.x).
  </Card>
  <Card title="Template size budget" icon="sliders">
    the validator now measures the compact config payload (what is POSTed on save) and errors above 100 KB / warns above 90 KB against the AIOStreams 102,400-byte hard limit; oversized templates are trimmed to stay under budget.
  </Card>
  <Card title="Legacy torbox-search CI gates" icon="gauge">
    the template validator rejects torbox-search outside the Legacy lane and the configurator test suite fails if the generators ever emit it again.
  </Card>
  <Card title="Newznab/Torznab presets use the v2.32 single-endpoint shape" icon="wand">
    full api.url (usually ending in /api) with apiKey inside the api object; legacy newznabUrl/apiPath/checkOwned/seasonPackStrategy options are no longer emitted. The validator flags any remaining legacy NAB keys and any preset pointing at the shut-down TorBox Search API.
  </Card>
  <Card title="Pre-install payload guard" icon="layers">
    Direct Install now blocks configs over AIOStreams\u2019 100 KB (102,400-byte) save limit with a clear message instead of a generic 413 error.
  </Card>
  <Card title="Docs links consolidated on corebuilds-docs.docsalot.dev" icon="bug">
    README, configurator footer, tools page, and generated docs no longer point at the stale mintlify mirror.
  </Card>
  <Card title="Express Install lane" icon="sparkles">
    one-click two-step install (pick debrid → connect Stremio or grab the manifest) with Balanced defaults and Full-Stack AIOMetadata + Cinemeta patch by default. Reuses the shared install pipeline; no new machinery.
  </Card>
  <Card title="Core Score" icon="bolt">
    every stream in Test Drive now shows an explainable 0–100 quality badge (quality tier, bitrate vs IQR fence, resolution fit, HDR, source trust, seeders, freshness) with a tap-to-open breakdown of every gate. Flagship formatters add a branded Core {score} badge.
  </Card>
  <Card title="Fixed: Debridio/Debrider config reject" icon="shield-check">
    the presets were emitted enabled without their required API key, so AIOStreams rejected the config (\'debridioApiKey is required, got undefined\'). They are now omitted until a key is entered, with a preflight warning.
  </Card>
  <Card title="Express Install" icon="sliders">
    added an \'Additional services & scrapers\' popout (Debridio, Debrider, Usenet, indexers) mirroring Quick Install, with credentials folding into the same flow.
  </Card>
  <Card title="Stronger update system" icon="gauge">
    live check for newer template versions (cached, rate-limited, fail-silent offline, credential-free), a one-click \'Update\' that pulls the latest template through the diff/apply flow, a changelog excerpt in the update banner, and a Revert action (pre-update state is backed up). The host-preload template collection is now generated from the active suite (scripts/sync_template_collection.py) and CI-guarded so it never goes stale (TEMPLATE_URLS documented in STATUS.md).
  </Card>
  <Card title="Fixed: regex whitelist confusion (#658)" icon="wand">
    the host-compatibility check now names the regex patterns not on the ElfHosted/ForTheWeak allowlist (from the Vidhin05 snapshot) and offers a one-click \'Strip non-allowlisted regex patterns\' action, so users no longer see \'remove those 6\' with no idea which or how.
  </Card>
  <Card title="New: Usenet Only service" icon="layers">
    dedicated path for pure-usenet setups (no debrid). Enables stremio_nntp + aiostreams built-in engine, bundles EasyNews++, EasyNews Search, NZBGeek, and all Newznab indexers. NZBHydra2 meta-search scraper added to optional scrapers.
  </Card>
</CardGroup>

---

## Configurator v2.89 (Aug 4 2026)

<CardGroup cols={2}>
  <Card title="Fixed: episode-pack availability" icon="sparkles">
    multi-episode and season-pack streams were removed too early, before cache and quality filters ran; when those filters eliminated single-episode alternatives, valid requests could return zero streams. Packs are now hidden only after three playable standalone episodes survive all filtering.
  </Card>
  <Card title="Fixed: Core Tools hub restored" icon="bolt">
    the complete tools page is back after a truncated deployment removed cards and closing markup
  </Card>
</CardGroup>

---

## Configurator v2.88 (Aug 3 2026)

<CardGroup cols={2}>
  <Card title="Fixed: SubDL language schema rejection" icon="sparkles">
    uses singular language field with uppercase provider codes, capped at 5 (#638)
  </Card>
  <Card title="Fixed: Debridio missing in multi-service configurations" icon="bolt">
    preset now emits when multiServices includes debridio (#639)
  </Card>
  <Card title="Fixed: Peerflix schema rejection" icon="shield-check">
    useMultipleInstances added to all generated presets
  </Card>
  <Card title="Import safety" icon="sliders">
    credentials stripped from all public paste uploads before leaving the browser
  </Card>
  <Card title="Worker TEMPLATES KV binding" icon="gauge">
    wrangler.toml now carries the active KV namespace for paste storage
  </Card>
  <Card title="Account Manager promoted to available" icon="wand">
    tools page card now links directly to the Account Manager
  </Card>
  <Card title="Security: replaced Math.random() with crypto.getRandomValues() for session IDs and account generation (CodeQL #408)" icon="layers">
    Security: replaced Math.random() with crypto.getRandomValues() for session IDs and account generation (CodeQL #408)
  </Card>
  <Card title="New CI workflow for shared core, CLI, account-tools, and Worker test suites" icon="bug">
    New CI workflow for shared core, CLI, account-tools, and Worker test suites
  </Card>
</CardGroup>
