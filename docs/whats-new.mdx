---
title: "What's New — Configurator v2.94"
sidebarTitle: "What's New"
description: "Latest Core Builds Configurator releases (v2.94 and earlier), auto-generated from the in-app changelog."
---

# What's New in the Configurator

This page is regenerated from `configurator/src/data/changelog.js` by `scripts/sync-docs.py`, so it always reflects the latest shipped configurator version (currently **v2.94**).

For the template-suite release log, see the [Changelog](https://corebuilds-docs.docsalot.dev/changelog).

## Configurator v2.94 (Aug 16 2026)

<CardGroup cols={2}>
  <Card title="Fixed (generation): the Jackett and Prowlarr preset builders sent a bare 'apiKey'" icon="sparkles">
    an option those presets never read — the same mistake as the Debridio fix in v2.93. Both now send the provider-specific key (jackettApiKey / prowlarrApiKey), and each preset is emitted only when its URL and key are both present, because AIOStreams rejects the entire config if either is missing. Confirmed against the AIOStreams preset definitions.
  </Card>
  <Card title="Known issue (documented, not yet changed): the Jackett and Prowlarr presets cannot currently be produced at all. Their emission is gated on an instance URL that no screen collects" icon="bolt">
    there is no input for it anywhere in the app — so selecting either indexer has never added it to a config. The option-name fix above is therefore correct-but-unreachable groundwork, not a change you can observe today. Adding the URL field is a separate decision.
  </Card>
  <Card title="Test integrity: the end-to-end suite now checks generated configs against AIOStreams' actual preset option contract instead of accepting whatever we send it. That gap is why the Debridio bug shipped green and reached users" icon="shield-check">
    two tests were asserting the wrong option name against a mock that validated nothing. Building the new check is what surfaced the Jackett and Prowlarr bugs above, before anyone hit them.
  </Card>
</CardGroup>

---

## Configurator v2.93 (Aug 15 2026)

<CardGroup cols={2}>
  <Card title="Fixed: Debridio configs were rejected by AIOStreams even when you HAD entered a key ('Option debridioApiKey is required, got undefined'). Core Builds was sending a bare apiKey" icon="sparkles">
    an option the Debridio preset never reads — so the host saw its required option as missing. The earlier 'omit the preset until a key is entered' change hid this: with no key the preset vanishes, so the rejection only ever reached people actually using Debridio. The option name is now confirmed against the AIOStreams preset source rather than inferred.
  </Card>
  <Card title="Fixed: the import and 'all hosts unreachable' panels rendered their step numbers and mid-sentence emphasis each on its own line, shredding the instructions. A styling rule intended for the panel heading was applying to every bold fragment inside it." icon="bolt">
    Fixed: the import and 'all hosts unreachable' panels rendered their step numbers and mid-sentence emphasis each on its own line, shredding the instructions. A styling rule intended for the panel heading was applying to every bold fragment inside it.
  </Card>
  <Card title="Known issue (documented, not yet changed): the Debrider preset is emitted with a type AIOStreams does not recognise" icon="shield-check">
    `debrider` is a service id, not one of the 76 preset ids the host accepts — so the host cannot resolve it. Recorded at the emission site pending a decision on the right fix.
  </Card>
</CardGroup>

---

## Configurator v2.92 (Aug 11 2026)

<CardGroup cols={2}>
  <Card title="AIOStreams v2.33.1 adopted same-day" icon="sparkles">
    the host fleet hardened its config validation on Aug 11 (required options for torrentio/peerflix/jackett/prowlarr/newznab/nzbhydra; presets enforcing usable-service deps). Core Builds generators, templates, and CI validator updated and proven against the live hosts within hours; a nightly fleet-version watcher is the follow-up.
  </Card>
  <Card title="Express lane truth-in-advertising pass" icon="bolt">
    a visible 'Streams shown' toggle (Mixed/⚡Cached only), an always-on install chorus (service → app · profile · resolution · cache mode · host), receipts always show the cache mode, and profile picks no longer stomp an explicit choice. Fixes the 'everything downloads to debrid' confusion class.
  </Card>
  <Card title="Nuvio instant path honors your AIOStreams host pick on Express (a compatible pick is used; an incompatible one gets a named, actionable error instead of a silent swap). Tutorial completion and the legacy quick-install lane now land in the consolidated Express lane." icon="shield-check">
    Nuvio instant path honors your AIOStreams host pick on Express (a compatible pick is used; an incompatible one gets a named, actionable error instead of a silent swap). Tutorial completion and the legacy quick-install lane now land in the consolidated Express lane.
  </Card>
  <Card title="Template imports get safe-credential hygiene everywhere: community templates ship presets that need your API key as disabled-with-a-note, share links never carry keys, and the update tool warns when an imported preset type can't be regenerated without re-adding its key." icon="sliders">
    Template imports get safe-credential hygiene everywhere: community templates ship presets that need your API key as disabled-with-a-note, share links never carry keys, and the update tool warns when an imported preset type can't be regenerated without re-adding its key.
  </Card>
  <Card title="New tool: Preflight (tools page)" icon="gauge">
    audit any template/config offline with rules learned from real host rejections, live dry-run the config against the actual AIOStreams host you target with a bounded auto-heal loop (until the host accepts), read-back proof of complete writes, and a manifest Doctor for existing installs. All of it browser-side through our proxy; nothing stored.
  </Card>
  <Card title="WuPlay Catalog Genie gains consent-gated live auto-apply" icon="wand">
    answers → a snapshot-first, per-step verified write of hub layouts, screen layouts and age gates to your WuPlay profile, one-tap Undo, and an honest tap-guide fallback for anything the API doesn't accept yet. PIN-locked profiles degrade to guided mode explicitly. Handshake probe + diagnostics lane included.
  </Card>
  <Card title="Quarantined a long-standing e2e flake properly: the Express 'Full Stack' chain now has a bounded 2-retry policy with explicit timeouts instead of wobbling the whole suite (#682). Fixed the Nuvio-import spec to express the honored-host contract." icon="layers">
    Quarantined a long-standing e2e flake properly: the Express 'Full Stack' chain now has a bounded 2-retry policy with explicit timeouts instead of wobbling the whole suite (#682). Fixed the Nuvio-import spec to express the honored-host contract.
  </Card>
  <Card title="Live instance-status cron retired" icon="bug">
    the docs page is now a guidance snapshot, and live per-host checks live in Preflight (Tools page), which tests your actual host against your actual config instead of a six-hours-stale ping list.
  </Card>
  <Card title="Genies graduate onto the main page" icon="sparkles">
    the Setup Genie door joins 'Choose your route' and the Nuvio Stack Genie joins the Tools row. Both pages dropped their prototype stubs: every button goes somewhere real, and finishing a genie hands your picks to the builder via a sessionStorage hand-off (Express lane opens pre-set — Nuvio/WuPlay pick the right target; a free pick preselects Free/P2P; Debrid-Link routes to Advanced). New deep-links: #express, #advanced, #update, #backup.
  </Card>
</CardGroup>

---

## Configurator v2.91 (Aug 9 2026)

<CardGroup cols={2}>
  <Card title="Fixed: 'Rebuild loop'" icon="sparkles">
    the version banner's Rebuild button wiped all settings but never stamped the current version, so the outdated banner returned after every rebuild. Now keeps settings, stamps the version, and toasts the user to download + re-import.
  </Card>
  <Card title="Fixed: explicit AIOStreams host selection is now honored" icon="bolt">
    picking a specific host (e.g. Omni, ForTheWeak) no longer silently fails over to a different host. Outdated hosts (< v2.32.0) get a clear version-gate error with remediation steps.
  </Card>
  <Card title="Express Install now shows a visible AIOStreams host dropdown (prefilled from Advanced), so the host choice is explicit from the start." icon="shield-check">
    Express Install now shows a visible AIOStreams host dropdown (prefilled from Advanced), so the host choice is explicit from the start.
  </Card>
</CardGroup>
