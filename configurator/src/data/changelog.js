export const CHANGELOG = [
  { v:'2.96', date:'Aug 19 2026', items:[
    'CoreSpeed, a TorBox CDN speed test — measures all 17 Hyperdrive nodes and tells you which one to be on. It tests them the way a player actually streams: one node at a time, over a single connection, capped at 10 seconds each (~75 to 180 seconds for the full set). Ping is the trimmed mean of 5 HEAD requests. Live gauge and sparkline while it walks the list, then a ranked table, world map, and a per-region verdict on what each node can really stream for you (4K Remux down to SD, with 15% headroom) — and because latency is measured honestly, a node above 250 ms cannot earn a 4K verdict no matter how fast it downloads. Last 15 runs are kept locally with change-vs-previous columns; export as Markdown, CSV or JSON. Find it at tools/speedtest/ or from the Builder splash under Utilities.',
    'Sequential is the default, and it is the mode to trust. Burst, Quick and Full run every node at once with several streams each — that measures how much pipe you have, not what a stream will do, because parallel connections mask latency and a fast-ramping node steals bandwidth from its neighbours during the race. Those modes are labelled capacity races and deliberately produce no playback verdicts. Quick pulls 100 MB per node and Full pulls 1 GB, so a Full run moves real gigabytes; there is a hard 12 GB budget and an instant Stop for exactly that reason. No account, no API key, and no Core Builds backend: the measurement runs in your browser and results stay there.',
    'If a run ever looks wrong — a far-away node winning, or results that disagree with TorBox\'s own site — re-run in Sequential mode first, then use the ⛨ Diagnostics button and share the JSON it downloads. It carries per-node pings, every round, DNS records and your ISP/ASN, which is what the crew needs to tell a routing quirk from a bug.',
    'The proxy lane was opened narrowly — TorBox\'s API sends no CORS header, so CoreSpeed fetches the CDN list through the Core Builds worker. That worker forwards any Authorization header a caller sets, which the WuPlay lane depends on, and allowing TorBox under those rules would have turned it into an authenticated relay for a service where you hold your own account key. It is scoped instead to that single public read, with credentials stripped and every other path and method refused — verified against the deployed worker rather than assumed.',
  ]},
  { v:'2.95', date:'Aug 16 2026', items:[
    'Stream scoring is live on Advanced and Labs — until now every stream carried the same score of nothing. AIOStreams only fills in a stream\'s score from a "ranked expression" list, Core Builds never sent one, and so the score-based sort key and the Adaptive Score Floor were quietly dropped from every config we generated. Both were listed as features. Neither had ever run. Advanced and Labs configs now ship a set of scored rules, so the score they sort on is real.',
    'Quality now adds up instead of laddering — resolution, quality, HDR/Dolby Vision, audio format and release-group reputation each contribute points, while low-quality groups and upscales subtract them. Because the points combine, a 4K remux with Atmos from a top group outranks a plain 4K remux without anyone writing a rule for that exact pairing, which is what the old tier ladder required. Points for Dolby Vision and lossless audio are awarded only on devices that can play them.',
    'Stable and Balanced are unaffected — both clear the scoring layer on purpose, so their configs are unchanged in content. The only difference is one empty field that now appears earlier in the file. This lands solely where you have opted into the deeper filtering.',
  ]},
  { v:'2.94', date:'Aug 16 2026', items:[
    'Fixed (generation): the Jackett and Prowlarr preset builders sent a bare "apiKey" — an option those presets never read — the same mistake as the Debridio fix in v2.93. Both now send the provider-specific key (jackettApiKey / prowlarrApiKey), and each preset is emitted only when its URL and key are both present, because AIOStreams rejects the entire config if either is missing. Confirmed against the AIOStreams preset definitions.',
    'Known issue (documented, not yet changed): the Jackett and Prowlarr presets cannot currently be produced at all. Their emission is gated on an instance URL that no screen collects — there is no input for it anywhere in the app — so selecting either indexer has never added it to a config. The option-name fix above is therefore correct-but-unreachable groundwork, not a change you can observe today. Adding the URL field is a separate decision.',
    'Test integrity: the end-to-end suite now checks generated configs against AIOStreams\' actual preset option contract instead of accepting whatever we send it. That gap is why the Debridio bug shipped green and reached users — two tests were asserting the wrong option name against a mock that validated nothing. Building the new check is what surfaced the Jackett and Prowlarr bugs above, before anyone hit them.',
  ]},
  { v:'2.93', date:'Aug 15 2026', items:[
    'Fixed: Debridio configs were rejected by AIOStreams even when you HAD entered a key ("Option debridioApiKey is required, got undefined"). Core Builds was sending a bare apiKey — an option the Debridio preset never reads — so the host saw its required option as missing. The earlier "omit the preset until a key is entered" change hid this: with no key the preset vanishes, so the rejection only ever reached people actually using Debridio. The option name is now confirmed against the AIOStreams preset source rather than inferred.',
    'Fixed: the import and "all hosts unreachable" panels rendered their step numbers and mid-sentence emphasis each on its own line, shredding the instructions. A styling rule intended for the panel heading was applying to every bold fragment inside it.',
    'Known issue (documented, not yet changed): the Debrider preset is emitted with a type AIOStreams does not recognise — `debrider` is a service id, not one of the 76 preset ids the host accepts — so the host cannot resolve it. Recorded at the emission site pending a decision on the right fix.',
  ]},
  { v:'2.92', date:'Aug 11 2026', items:[
    'AIOStreams v2.33.1 adopted same-day — the host fleet hardened its config validation on Aug 11 (required options for torrentio/peerflix/jackett/prowlarr/newznab/nzbhydra; presets enforcing usable-service deps). Core Builds generators, templates, and CI validator updated and proven against the live hosts within hours; a nightly fleet-version watcher is the follow-up.',
    'Express lane truth-in-advertising pass — a visible "Streams shown" toggle (Mixed/⚡Cached only), an always-on install chorus (service → app · profile · resolution · cache mode · host), receipts always show the cache mode, and profile picks no longer stomp an explicit choice. Fixes the "everything downloads to debrid" confusion class.',
    'Nuvio instant path honors your AIOStreams host pick on Express (a compatible pick is used; an incompatible one gets a named, actionable error instead of a silent swap). Tutorial completion and the legacy quick-install lane now land in the consolidated Express lane.',
    'Template imports get safe-credential hygiene everywhere: community templates ship presets that need your API key as disabled-with-a-note, share links never carry keys, and the update tool warns when an imported preset type can\'t be regenerated without re-adding its key.',
    'New tool: Preflight (tools page) — audit any template/config offline with rules learned from real host rejections, live dry-run the config against the actual AIOStreams host you target with a bounded auto-heal loop (until the host accepts), read-back proof of complete writes, and a manifest Doctor for existing installs. All of it browser-side through our proxy; nothing stored.',
    'WuPlay Catalog Genie gains consent-gated live auto-apply — answers → a snapshot-first, per-step verified write of hub layouts, screen layouts and age gates to your WuPlay profile, one-tap Undo, and an honest tap-guide fallback for anything the API doesn\'t accept yet. PIN-locked profiles degrade to guided mode explicitly. Handshake probe + diagnostics lane included.',
    'Quarantined a long-standing e2e flake properly: the Express "Full Stack" chain now has a bounded 2-retry policy with explicit timeouts instead of wobbling the whole suite (#682). Fixed the Nuvio-import spec to express the honored-host contract.',
    'Live instance-status cron retired — the docs page is now a guidance snapshot, and live per-host checks live in Preflight (Tools page), which tests your actual host against your actual config instead of a six-hours-stale ping list.',
    'Genies graduate onto the main page — the Setup Genie door joins "Choose your route" and the Nuvio Stack Genie joins the Tools row. Both pages dropped their prototype stubs: every button goes somewhere real, and finishing a genie hands your picks to the builder via a sessionStorage hand-off (Express lane opens pre-set — Nuvio/WuPlay pick the right target; a free pick preselects Free/P2P; Debrid-Link routes to Advanced). New deep-links: #express, #advanced, #update, #backup.',
  ]},
  { v:'2.91', date:'Aug 9 2026', items:[
    'Fixed: "Rebuild loop" — the version banner\'s Rebuild button wiped all settings but never stamped the current version, so the outdated banner returned after every rebuild. Now keeps settings, stamps the version, and toasts the user to download + re-import.',
    'Fixed: explicit AIOStreams host selection is now honored — picking a specific host (e.g. Omni, ForTheWeak) no longer silently fails over to a different host. Outdated hosts (< v2.32.0) get a clear version-gate error with remediation steps.',
    'Express Install now shows a visible AIOStreams host dropdown (prefilled from Advanced), so the host choice is explicit from the start.',
  ]},
  { v:'2.90', date:'Aug 6 2026', items:[
    'AIOStreams v2.32 compatibility — the legacy built-in torbox-search preset is removed from all generated configs and all 72 static templates (the TorBox Search API was shut down; saving a config that still includes it fails on v2.32+ hosts). No automatic conversion to the generic Newznab option is performed.',
    'Templates/Legacy/v2.31.1/ — explicit compatibility lane preserving the 15 previously-enabled templates with torbox-search for hosts still on AIOStreams 2.31.1 or older.',
    'Minimum AIOStreams version raised to 2.32.0 — host auto-select and version floor now match the current public host fleet (all stable instances are v2.32.x).',
    'Template size budget — the validator now measures the compact config payload (what is POSTed on save) and errors above 100 KB / warns above 90 KB against the AIOStreams 102,400-byte hard limit; oversized templates are trimmed to stay under budget.',
    'Legacy torbox-search CI gates — the template validator rejects torbox-search outside the Legacy lane and the configurator test suite fails if the generators ever emit it again.',
    'Newznab/Torznab presets use the v2.32 single-endpoint shape — full api.url (usually ending in /api) with apiKey inside the api object; legacy newznabUrl/apiPath/checkOwned/seasonPackStrategy options are no longer emitted. The validator flags any remaining legacy NAB keys and any preset pointing at the shut-down TorBox Search API.',
    'Pre-install payload guard — Direct Install now blocks configs over AIOStreams\u2019 100 KB (102,400-byte) save limit with a clear message instead of a generic 413 error.',
    'Docs links consolidated on corebuilds-docs.docsalot.dev — README, configurator footer, tools page, and generated docs no longer point at the stale mintlify mirror.',
    'Express Install lane — one-click two-step install (pick debrid → connect Stremio or grab the manifest) with Balanced defaults and Full-Stack AIOMetadata + Cinemeta patch by default. Reuses the shared install pipeline; no new machinery.',
    'Core Score — every stream in Test Drive now shows an explainable 0–100 quality badge (quality tier, bitrate vs IQR fence, resolution fit, HDR, source trust, seeders, freshness) with a tap-to-open breakdown of every gate. Flagship formatters add a branded Core {score} badge.',
    'Fixed: Debridio/Debrider config reject — the presets were emitted enabled without their required API key, so AIOStreams rejected the config (\"debridioApiKey is required, got undefined\"). They are now omitted until a key is entered, with a preflight warning.',
    'Express Install — added an \"Additional services & scrapers\" popout (Debridio, Debrider, Usenet, indexers) mirroring Quick Install, with credentials folding into the same flow.',
    'Stronger update system — live check for newer template versions (cached, rate-limited, fail-silent offline, credential-free), a one-click \"Update\" that pulls the latest template through the diff/apply flow, a changelog excerpt in the update banner, and a Revert action (pre-update state is backed up). The host-preload template collection is now generated from the active suite (scripts/sync_template_collection.py) and CI-guarded so it never goes stale (TEMPLATE_URLS documented in STATUS.md).',
    'Fixed: regex whitelist confusion (#658) — the host-compatibility check now names the regex patterns not on the ElfHosted/ForTheWeak allowlist (from the Vidhin05 snapshot) and offers a one-click \"Strip non-allowlisted regex patterns\" action, so users no longer see \"remove those 6\" with no idea which or how.',
    'New: Usenet Only service — dedicated path for pure-usenet setups (no debrid). Enables stremio_nntp + aiostreams built-in engine, bundles EasyNews++, EasyNews Search, NZBGeek, and all Newznab indexers. NZBHydra2 meta-search scraper added to optional scrapers.',
  ]},
  { v:'2.89', date:'Aug 4 2026', items:[
    'Fixed: episode-pack availability — multi-episode and season-pack streams were removed too early, before cache and quality filters ran; when those filters eliminated single-episode alternatives, valid requests could return zero streams. Packs are now hidden only after three playable standalone episodes survive all filtering.',
    'Fixed: Core Tools hub restored — the complete tools page is back after a truncated deployment removed cards and closing markup',
  ]},
  { v:'2.88', date:'Aug 3 2026', items:[
    'Fixed: SubDL language schema rejection — uses singular language field with uppercase provider codes, capped at 5 (#638)',
    'Fixed: Debridio missing in multi-service configurations — preset now emits when multiServices includes debridio (#639)',
    'Fixed: Peerflix schema rejection — useMultipleInstances added to all generated presets',
    'Import safety — credentials stripped from all public paste uploads before leaving the browser',
    'Worker TEMPLATES KV binding — wrangler.toml now carries the active KV namespace for paste storage',
    'Account Manager promoted to available — tools page card now links directly to the Account Manager',
    'Security: replaced Math.random() with crypto.getRandomValues() for session IDs and account generation (CodeQL #408)',
    'New CI workflow for shared core, CLI, account-tools, and Worker test suites',
  ]},
  { v:'2.87', date:'Aug 2 2026', items:[
    'Nuvio + TorBox Instant Quick Install — dedicated flow generates a P2P-only template with device/resolution/host pickers and TorBox Connected Services warning',
    'Nuvio TorBox Instant CLI route — core-builds generate --route nuvio-torbox-instant --host fortheweak --device shield --resolution 4k',
    'Nuvio host capability metadata — supportsNuvioInstant flag on all hosts; ElfHosted correctly blocked',
    'Core Builds CLI published to npm — npm install -g core-builds for generate, validate, diff, and info',
    'Account Manager mutations — addon reorder, removal, backup restore, and bounded undo with stale-state detection',
    'Capability-based device profiles — Android Mobile, Android TV / Google TV, Samsung Tizen, LG webOS, Sony / Google TV, and Generic 4K HDR TV with device-specific warnings and help text',
    'Matching Core Builds SVG device icons replace temporary emoji icons in the device picker',
    'Expanded desktop and mobile Playwright E2E coverage for device card visibility, selection, keyboard, icons, and help text',
    'Fixed: Max File Size control was setting the selected limit as the minimum size; now correctly caps at the selected maximum',
    'Fixed: Legacy unversioned local snapshots no longer show a false "Update Available" banner',
    'Fixed: Template updates no longer mutate the active configuration before confirmation',
    'Fixed: Feedback messages now report current live setup context instead of stale saved state',
    'Fixed: Fine-Tune interaction and focus regressions are covered by browser tests',
    'SEL generation now uses shared Standard, IQR, Apex, and Apex Mixed policy modules',
    'CodeQL now uses the repository configuration and excludes generated bundles while scanning source code',
  ]},
  { v:'2.86', date:'Jul 29 2026', items:[
    'Hotfix: Max File Size control was setting the selected limit as the minimum size (e.g. 10GB → allowed 10–999999GB); now correctly caps at the selected maximum (e.g. 10GB → 1B–10GB)',
    'SEL Engine v2 — perGroup() QR balance replaces 8 slice expressions with 2 (interleaved results, device-aware limits); Addon Diversity cap; Bitrate Anomaly Pin (IQR outliers → bottom, not removed)',
    'Adaptive Score Floor — replaces static Low SEL Score; threshold scales with content age (strict on new releases, lenient on niche/old content)',
    'Dynamic addon groups — built from actual enabled presets using instanceId; Primary/Secondary split with cached-aware conditions; fixes "Every group must have at least one addon" rejection',
    'Soft-fail addon recovery — Config rejected with "Failed to fetch manifest" now parses the error, identifies the addon, offers disable+retry; auto-backups config before recovery',
    '?simulateAddonFail= self-test hook — append ?simulateAddonFail=StremThru Torz TB:fetch failed to URL to test recovery UI in-browser',
    'Bandwidth Mbps cap — type your internet speed in Fine-Tune, bitrate auto-limits to 80%',
    'Inspector direct import — drag & drop JSON files or click 📁 to browse; unwrap() normaliser handles all JSON shapes (template, bare config, array)',
    'New ISEs — Protect Library & SeaDex (single passthrough shields from all filters), ID-Matched Trust (external-ID streams skip filename matching), Smart Play Pin (🎯 → top)',
    'Low Seeder Cull — removes 0-seeder streams only when 3+ healthy streams exist (protects niche content)',
    'RD Copyright ESE — preemptively filters WEB-DL/BDRip on RealDebrid (DMCA compliance)',
    'Fixed: tools/account-tools links broken from Configurator (relative path ./tools/ → ../tools/)',
    'Fixed: NC-17 label read backwards ("No Restriction" → "Adults Only")',
    'Fixed: instanceUrl unescaped in DOM (XSS vector)',
    'Fixed: CodeQL #158 — RADIO_ALLOWED allowlist guard on radio inputs',
    'Fixed: UI layering — import dialog z-index, info card text clipping',
    "Docs auto-sync — ROADMAP.md + tools What's New auto-generated from changelogs; changelog gate blocks merges without update",
    'Vidhin05 drift watch — daily cron detects upstream regex/expression changes, opens issue with diff',
  ]},
  { v:'2.85', date:'Jul 26 2026', items:[
    'Mixed · Adaptive resolution tier — new fourth resolution option: no hard caps, 4K/1440p/1080p/720p/SD all eligible, ranked by a cached × quality blend; dedicated PSE ladder (4K REMUX/HDR → 1080p → 720p → 576p/480p niche fallback), UHD-class regex tiers, blended dynamic-fetch exit, and import round-trip detection',
    'Apex Mixed PSE architecture — third architecture option (Standard / Apex IQR / Apex Mixed): the flagship 35-PSE IQR × adaptive stack from the 4K Apex Mixed nightly, generatable for any device/service; adopts the mixed resolution policy (no caps, quality-blend sort, blended DAF exit) and names builds "Core Nexus Apex Mixed". Golden-link test keeps it byte-identical to the static template',
    'Golden config snapshots — Playwright e2e drives the real generation pipeline across 12 service × resolution × architecture combinations and diffs full template output against checked-in goldens (e2e/golden/); any logic change that alters generated templates now fails CI, with UPDATE_GOLDEN=1 for intentional re-pins',
    'Fixed: Jackett & Prowlarr optional scraper presets were never generated (regression of the #557 filter fix, lost in the v2.84 modular rewrite) — restored `credKey && !apiUrl` guard',
    'Regression tests: optional-scrapers branch coverage (every scraper maps to exactly one preset branch) and resolution-mixed generation guards',
    'Fixed: template validator crashed on non-template JSON (e.g. Filtering/ expression libraries) when run without --dir arguments',
  ]},
  { v:'2.84', date:'Jul 25 2026', items:[
    'Error Logger wired — initErrorLogger() at startup, logError() on build/deploy/import failures, error log section in Diagnostics modal with copy/clear/export',
    'Age Rating Limit — Restrict content by MPAA/TV certification (G, PG, PG-13, R) via ESE; added to Video Preferences panel',
    'Library Boost — Three modes in Fine-Tune: Default (first within tier), Strong (always top), None (disabled); modifies sort criteria across all scopes',
    'NZB Failover — Configurable position (before/after torrents) and max NZB count (1/2/3/5) in Fine-Tune; shown when Usenet service selected',
    'Bandwidth Estimator — Device-aware bandwidth hint card in Review step showing recommended speed and safe bitrate cap',
  ]},
  { v:'2.83', date:'Jul 25 2026', items:[
    'Error Logger — Captures build, deploy, fetch, and unhandled errors into a localStorage ring buffer (50 entries); view, copy, and clear from the Diagnostics modal',
    'Live Contact Widget — Floating message button routes feedback directly to Discord via the CORS proxy worker (no secrets in client JS)',
    'Worker contact endpoint — Server-side Discord webhook relay added to the Cloudflare Worker',
  ]},
  { v:'2.82', date:'Jul 24 2026', items:[
    'Patch Cinemeta & AIOMetadata — opt-in toggles in Full Setup for patching Cinemeta catalogs and installing AIOMetadata',
    'Full Setup service selection — Full Setup now reads the active splash chip and sets the correct service/multiServices',
    'Library Boost — Three modes: Default (normal position), Strong (top priority), None (disabled)',
    'Bandwidth Estimator — Device-aware bandwidth suggestion with manual override (10–200 Mbps)',
    'Age Rating Limit — MPAA certification filter (G / PG / PG-13 / R) powered by TMDB metadata',
    'NZB Failover — Configurable position (before/after torrents) and max NZB count (1/2/3/5)',
    'Template Inspector upgraded — File import, URL fetch, clipboard paste, health score ring, host compatibility checks, export report, theme toggle',
  ]},
  { v:'2.81', date:'Jul 24 2026', items:[
    'Health Score — Added a template health score ring to the Review step with breakdown by sort criteria, ISEs, ESEs, formatter, regex, and deduplicator',
    'Template Versioning — Templates now embed coreBuildsVersion and generatedAt metadata; an update banner appears when a saved template is outdated',
    'Full Stack Install — One-click setup now installs AIOMetadata and patches Cinemeta catalogs alongside AIOStreams, with addon reordering',
    'CORS proxy — Config push to AIOStreams routed through writeHostFetch for cross-origin reliability',
    'Tools hub — Added links to Core Tools from Review step, Quick Install, and manifest modal',
    'Debug modules — Wired error boundary, state guard, network resilience, config validator, sanitized logger, and template import recovery',
    'family-v4 formatter — New default formatter with bitrate, release group, seeders, age, indexer, and season pack info; family-v3 demoted to Legacy',
  ]},
  { v:'2.78', date:'Jul 22 2026', items:[
    'Modular architecture — Migrated from monolithic single-file source to ES modules under src/ with esbuild bundling',
    'Build pipeline — Added standalone (all-inlined) and web (cache-friendly) distribution builds',
    'Testing — Added unit tests for schema guard, device profiles, and host metadata',
    'Validation — Added static module validation for release gating',
  ]},
  { v:'2.77', date:'Jul 22 2026', items:[
    'Quick Install — Added a Duck-inspired one-page flow for app, service, contextual credentials, and Fast/Balanced/Maximum performance profiles',
    'Progressive disclosure — Stremio login, debrid API keys, and EasyNews credentials appear only when the selected path needs them',
    'Reliability — The last successful AIOStreams host is checked first, stable hosts are preferred, outdated versions are skipped, free/P2P avoids incompatible hosts, and nightly is used only as fallback',
    'Clean install — Added an opt-in replacement mode for older manifests from known public AIOStreams hosts during direct Stremio installation',
    'Diagnostics & preflight — Added a sanitized issue report plus template structure, duplicate preset, health, credential, and host compatibility checks',
    'Unified menus — Applied the splash screen’s glass surfaces, editorial headings, cyan interaction language, forms, navigation, sidebar, review cards, receipts, and modal treatment throughout',
    'Visual polish — Simplified wizard gradients, shadows, selected states, animation, spacing, sidebar width, muted-text contrast, light mode, and the phone Quick Install sheet',
    'Mode control — Moved the light/dark control away from the Core Builds wordmark and added safe spacing above phone navigation',
    'Light mode — Rebuilt selected, active, hover, form, receipt, formatter, scraper, service, profile, and fine-tune states with pale-blue surfaces and explicit dark text',
    'Device profiles — Centralized audio, AV1, and Dolby Vision capabilities so selection, review, sorting, PSEs, ESEs, and diagnostics use the same rules',
    'Apple TV & Apple mobile — Removed incorrect TrueHD passthrough and A15 AV1 assumptions; newer-chip AV1 support is handled conservatively',
    'Chromecast, smart TVs, ONN & Roku — Corrected model-dependent AV1, Dolby Vision, HDR, internal-app lossless-audio, and passthrough assumptions',
    'Fire TV & Xiaomi — Added generation-aware audio guidance and corrected Xiaomi TV Box S 2nd Gen AV1 support',
    'Popularity — Reordered featured devices toward Android/Google TV, ONN, Shield, Fire TV 4K Max, and Samsung using Core Builds samples, Stremio community usage, and broader CTV adoption',
    'Multi-provider setup — Quick Install now supports selecting multiple debrid/Usenet/P2P providers and renders all required credentials contextually',
    'Additional sources — Added one shared popup for Quick Install and Guided/Advanced Sources; optional paid extras create contextual credential fields and selected-count summaries',
    'Naming — Standardized beginner-facing routes and steps as Guided Setup, Advanced Builder, Update Existing Setup, Ready-Made Setups, Sources, Video Quality, Accounts & Keys, and Review & Install',
    'Startup & state — Reduced the fixed loader from three seconds to under one second and added schema migration for corrected device defaults and newly saved settings',
    'Tutorial alignment — Rebuilt onboarding around Quick Install, multi-provider Sources, Ready-Made Setups, Guided Setup, Advanced Builder, validation, and Review & Install with working spotlight targets',
    'Credential transparency — Replaced inaccurate zero-knowledge wording with explicit local-export versus chosen-host Direct Install behaviour',
    'Schema guard — Added final enum sanitization for autoPlay attributes and cacheAndPlay stream types, including safe aliases and deduplication, to prevent legacy invalid-value import errors',
    'Architecture — Split development source into data, capability, schema, style, vendor, and application modules while retaining a single standalone release build and adding a cache-friendly web build',
    'Testing — Added executable schema-guard, device-capability, host-policy, and static module validation to the release pipeline',
  ]},
  { v:'2.75', date:'Jul 21 2026', items:[
    'Unified UI — Extended the approved hybrid visual system across every configurator step, option menu, form, review panel, sidebar, modal, receipt, and navigation control',
    'Desktop — Refined the fixed workflow rail, widened content canvas, clarified active-step hierarchy, and standardized glass surfaces and spacing',
    'iPhone — Added safe-area handling for notches and home indicators, 16px form controls to prevent Safari input zoom, compact progress navigation, and keyboard-friendly sticky actions',
    'Android — Added larger touch targets, tap-highlight cleanup, compact-device breakpoints, horizontal picker sizing, and dynamic viewport-height handling for browser chrome and keyboards',
    'Responsive UI — Added dedicated phone, small-phone, landscape, tablet, desktop, and standalone-PWA behavior while preserving light and dark themes',
    'Debugging — Validated application JavaScript syntax, HTML structure, standalone QR-library build, required element IDs, delegated actions, and ZIP integrity',
  ]},
  { v:'2.74', date:'Jul 21 2026', items:[
    'Reference Template Parity — generated templates now match the flagship Core Nexus 4K Apex output quality',
    'ESE Stack — Added 12 new ESEs: Extra SeaDex, Bad 4K Anime, Upscaled 4K, Bad 4K/1080P Bluray, AI Upscale Exclusion, Info & Unwanted, Low SEL Score, G\'s Low Bitrate, Low Seeders, Final Limit, Indexer Diversity, RD Copyright (per DMM)',
    'ISE Stack — Added Library fallback, digitalRelease Bypass, 0Cached passthrough, enhanced REPACK/PROPER Passthrough',
    'PSE Stack — Added per-quality/resolution slice limits (8 entries for 4K, 5 for 1080p), IMAX pin',
    'Config — Added hideErrors, digitalReleaseFilter, size/bitrate per-resolution limits, autoPlay, precache/preload, cacheAndPlay, nzbFailover, areYouStillThere, syncedRankedStreamExpressionUrls, resultLimits',
    'ESE — HdHub added to Per-Addon Flood Guard, ongoingSeasonPack uses onlySeasons mode, Hard Season Pack Kill with anime exemption',
    'Fix — “All addons must belong to a group” error: P2P/HTTP groups now dynamically include all preset addon names (subtitles, catalogs, optional scrapers)',
    'Fix — Removed SCR/TC/TS/CAM/Unknown from preferredQualities (low-quality sources should not be preferred)',
    'Landing UI — Rebuilt the start screen as a cohesive hybrid of Precision Dark structure, editorial typography, Aurora glass depth, Bento-style presets, and compact technical microcopy',
    'Support — Removed the floating heart and footer support buttons; the central Core gem is now the compact, keyboard-accessible support entry point with a pulsing glow, expanding signal ring, tooltip, and one subtle caption',
    'Usage counter — Retained live visit and template-generation statistics on the redesigned landing page',
    'Community — Renamed visible Reddit links to “Core Crew” while retaining the existing community destination',
    'Responsive UI — Added purpose-built desktop, tablet, and mobile layouts for the new hero, workflow routes, service selector, session-resume panel, and quick-deploy grid',
    'Compatibility — Preserved service selection, guided and advanced setup, template migration, saved-session resume, quick reinstall, comparison, changelog, usage counters, and all existing preset actions',
  ]},
  { v:'2.73', date:'Jul 21 2026', items:[
    'Ecosystem — Moved support into the central Core logo, removed the floating heart and footer support buttons, and renamed the Reddit community link to Core Crew',
    'UI Revamp — Upgraded the entire configurator layout to a breathtaking, translucent glassmorphic style. All step cards and the splash panel now render as premium frosted glass with high-end backdrop blurs, clean border outlines, and rich glowing cyan-purple radial background gradients that move beautifully behind the glass layers',
    'Diagnostics — Added new automated Health Check rules on the final step to instantly alert users if they enabled EasyNews or SubDL but forgot to enter their credentials',
    'Ecosystem — Integrated cutting-edge Stream Expression Language (SEL) boosters globally: Sub-First Anime Booster, Dolby Priority for Sonos/Bose bars, Incomplete Ongoing Show filter, and Clutter-Free Single Episode filter',
    'Fix — Overhauled Stremio Account authentication pathways (Login / Registration) to query the distinct sub-endpoints directly (/api/login and /api/register), successfully resolving the 404 router failures caused by Stremio’s recent base API path deprecations',
    'Security — Added a prominent green "Zero-Knowledge Security Shield" reassurance banner at the top of Accounts & Keys explaining that credentials never leave their browser and are compiled 100% locally',
    'Security — Added a "Safe Auth" green shield warning beneath the Stremio credentials on the direct-install panel, assuring users that accounts are used locally and never stored',
    'Redesign — Retires the device-selection select dropdown and featured list, replacing them with a highly polished side-scrolling Device Carousel showing specifications, capabilities, and specs guides directly inside expandable cards',
    'Navigation — Added a Changelog modal link next to "Start Over" on Step 6 to make reading version histories highly discoverable',
    'Ecosystem — Added a direct integration link to crispyduck.xyz (the community’s standard visual layout sandbox) on the custom formatter import panel',
    'Fix — Added missing movieHashPlusAutoAdjustment option to OpenSubtitles v3+ preset to prevent AIOStreams validation rejections',
    'Fix — Corrected Nuvio Streams preset to be disabled by default in HTTP and extra-HTTP lists to prevent manifest parsing crashes due to missing custom URLs',
    'Fix — Overhauled template import and URL loading to fully restore subtitle addons, subtitle languages, catalogs, proxy, and deduplication preferences into state',
    'Fix — Resolved a schema issue where deduplicator.merge option was not correctly serialized according to newer AIOStreams specifications',
    'Fix — Updated Debridio API URL to point to the active debridio.com domain instead of the defunct debrid.io',
    'Fix — Optimized SubDL credentials panel to direct users straight to the active subdl.com/panel/api page',
    'UX — Context-aware copy notifications show "Copied to clipboard" instead of "Manifest URL copied" when copying keys and passwords',
    'UX — Tactile micro-interactions for copy buttons: backgrounds glow emerald green and text transitions to "✓ Copied" on success',
    'UX — Smooth auto-scrolling to newly active credentials panels when toggling subtitle providers like SubDL',
    'Mobile UX — Added select-all-on-focus to all text/password fields to eliminate copy-paste friction on iOS',
    'Mobile UX ... Added programmatic selection highlighting on manual copy nodes to prevent text clipping in WebKit browsers',
    'Network — Live "Test" stream check now races requests through CORS proxy to eliminate browser cross-origin failures on public hosts',
  ]},
  { v:'2.72', date:'Jul 18 2026', items:[
    'Usage counter — splash page shows live visit count and templates built when a counter endpoint is configured',
    'Visit beacon fires on page load; generate beacon fires on template download — both use sendBeacon for zero-latency tracking',
    'Cloudflare Worker + KV counter service included in configurator/worker/',
  ]},
  { v:'2.71', date:'Jul 18 2026', items:[
    'Tutorial expanded — Fine-Tune panel and Review & Install steps added as centered coach-mark cards (6 steps, was 4)',
  ]},
  { v:'2.70', date:'Jul 18 2026', items:[
    'Hybrid service option removed — select TorBox + Real-Debrid individually via multi-select instead. Imported hybrid templates auto-map to multi-service',
    'Extras carousel — P2P Free, HTTP Streams, NZBGeek, StreamNZB, and optional Newznab indexers shown as a side-scroll card picker with multi-select toggle',
    'Formatter picker — replaced dropdown menu with horizontal scroll carousel showing live previews of all formatters',
    'UX — smoother step transitions with scale + cubic-bezier easing',
    'UX — collapsible accordion sections on Review page (Tools, Formatter, JSON Preview)',
    'UX — dark/light theme polish: 30+ CSS custom properties replace hardcoded inline colors',
    'Mobile UX — grids (test drive tiles, hero sections, compare table) collapse to single column on extra-small screens (≤360px)',
    'Fix — Family v3 formatter: preloading and library fields now use conditional formatting instead of showing raw true/false',
    'Fix — Fine-Tune info popups now stay within viewport on mobile',
    'Fix — configurator blank page caused by syntax error in Fine-Tune tooltip text',
  ]},
  { v:'2.61', date:'Jul 17 2026', items:[
    'Onboarding Tutorial — interactive coach-mark walkthrough for first-time visitors with spotlight cutouts on key UI elements',
    'Tutorial auto-launches on first visit (skipped for returning users with saved state); accessible via the Tutorial preset on the splash screen',
    'Optional Scrapers — add extra Newznab indexers (NZBnoob, altHUB, Usenet Crawler, DrunkenSlug, NZBFinder) from a dropdown in the service selection step',
    'Subtitle Picker — choose subtitle sources (AIOSubtitle, OpenSubtitles v3+, SubDL) and select from 30 languages in the Fine-Tune panel',
    'Catalog Presets — add TMDB, Streaming, Anime, RPDB, and Torrent catalog addons from the Fine-Tune panel',
    'Proxy Configuration — enable MediaFlow proxy and select which debrid services to route through it',
    'Dedup Merge / Failover — toggle deduplicator merge mode to keep deduplicated streams as failover alternatives instead of discarding them',
    'Free Tier Overhaul — P2P templates now include Torrentio, Peerflix, and Sootio as primary scrapers with 3-tier addon grouping, smart auto-hide ESEs, and tuned deduplicator',
  ]},
  { v:'2.57', date:'Jul 16 2026', items:[
    'Resolution First toggle — 4K always ranks above 1080p/720p regardless of cache status',
    'Foreign Language Kill — hard-blocks streams not in your selected languages. Library and SeaDex exempt. Anime exempt',
    'Splash — staggered fade-in animations, smart service pre-selection, keyboard navigation, ARIA radiogroup for screen readers',
    'Progress — step dots show current selection on hover, "Auto-saved" indicator flashes after each state change',
    'Stream Pool Broadening — Normal now returns 30–35 results (was 20–25), Large 50, Max 75',
    'Host Compatibility Checker — pre-export validation against elfhosted, fortheweak, and other host whitelists',
    'Import Existing Config — paste any AIOStreams config JSON or URL to reverse-engineer settings back into the configurator',
    'Guided Troubleshooter — interactive decision tree for common issues (no streams, wrong quality, import errors)',
    'Stremio Deep Link — one-click stremio:// protocol link that installs the addon directly',
    'Template Backup Timeline — every generation snapshots to localStorage with timestamp, scroll through history and restore',
    'IQR PSE Architecture — "Apex IQR" quality mode uses statistical bitrate filtering (Tukey fences) matching the flagship 4K Apex template',
    'Anime addon stack — AnimeTosho, NekoBT, and Sootio presets added to generated templates',
    'Accessibility — prefers-reduced-motion disables all animations and transitions',
  ]},
  { v:'2.51', date:'Jul 15 2026', items:[
    'Built-in Usenet Engine — stremio_nntp and aiostreams services added to generated templates (v2.31.0)',
    'Selective Migration — per-section checkboxes in the diff modal let you cherry-pick which changes to apply',
    'Parent/Child Config — "Use Core Builds Base" option enables parent config inheritance',
    'Template Migration Tool — full diff of what changes before applying, with Apply & Upgrade / Cancel',
    'Device detection on import — Samsung, Xiaomi, Fire Stick, Apple TV, Shield, Google TV, and Windows auto-detected',
    'Test Drive — preview your streams before installing with your selected formatter',
    'Template Updater — fixed service detection, resolution detection, and state bleeding on import',
    'Sort criteria — added service key for multi-debrid, age/subtitle tiebreakers, cachedAnime/uncachedAnime sections',
    'Family Formatter v3 by RB3 is now the default formatter',
    'Fix deduplicator.merge schema — removed boolean field that newer AIOStreams versions reject',
  ]},
  { v:'2.40', date:'Jul 14 2026', items:[
    'Direct Install — enter Stremio email + password inline to push the addon straight to your library',
    '"Create random account" link generates a Stremio account in one click',
    'Changelog is now its own modal — click the version badge to open the full release history',
    'UI refresh — calmer, Duck-inspired look: single accent, glow/pulse animations retired, flat selection states',
    'Multi-select service rows use toggle switches instead of checkboxes',
    'Spelling — "Debrid.io" is now "Debridio" across the configurator',
  ]},

  { v:'2.36', date:'Jul 11 2026', items:[
    'Fix — Configurator-generated templates no longer cause "Invalid input error" on elfhosted/fortheweak',
    'Removed syncedExcludedRegexUrls (legacy URL blocked by host allowlists)',
    'Added 107 ranked regex patterns for proper regexScore sorting',
    'Added 8 excluded regex patterns and resolution-aware preferred regex patterns',
    'Fix — excludedRegexPatterns now emitted as plain strings (was objects)',
    'Free tier fix — P2P and HTTP templates now skip failed manifest API call and go straight to import flow',
  ]},
  { v:'2.34', date:'Jul 10 2026', items:[
    'Mobile responsiveness — enhanced 480px and new 360px breakpoints for small phones',
    'Keyboard navigation — arrow keys navigate option grids, visible focus rings, focus trapping in modals',
    'Template Comparison Tool — side-by-side viewer comparing PSEs, ESEs, sort keys, scrapers, codecs, and more',
    'SVG icon system — all UI emoji replaced with crisp vector icons (30+ icons)',
    'Formatter preview image added to Fine-Tune panel',
  ]},
  { v:'2.31', date:'Jul 9 2026', items:[
    'All 17 formatters now show unique live previews — previously 12 of 17 fell back to the Apex V2 preview',
    'All 17 formatter entries added to the formatter picker (was 6)',
    'New services — EasyDebrid, PikPak, and Seedr added with StremThru Store integration',
    'AIOStreams v2.30.6 service sync — nzbdav, altmount, stremthru_newz added (15 total)',
  ]},
  { v:'2.29', date:'Jul 8 2026', items:[
    'Splash screen revamp — card-based layout with two-door entry (Guided Setup / Core Configure), quick deploy presets, service chip selector',
    'Rebrand: "Easy Setup" → "Core Start", "Custom Setup" → "Core Configure", "Advanced Options" → "Fine-Tune"',
    'Service picker redesigned — search filter, segmented category tabs (Debrid / Usenet / No Account), auth badges per row',
    'Credential Test buttons — verify API keys before installing, live validation in pre-flight checks',
    'Smart device detection — auto-selects your device from user agent (Fire Stick, Samsung, Apple TV, Shield)',
    'Template health check on review step — warns about missing guards, TMDB keys, and device mismatches',
    'Stream Pool option — Normal / Large / Maximum controls how many streams are fetched and returned',
    'Quality Priority toggle — promotes source quality (REMUX) above resolution in sort order',
    'Share links (#cfg=) now jump straight to review — one-tap import',
    'Free service fix — P2P/HTTP templates no longer include debrid-specific ESEs, PSEs, or sort keys',
    'Fix: "Create & Install" failing — auto-disables title/year matching when no TMDB key is provided',
  ]},
  { v:'2.19', date:'Jul 7 2026', items:[
    'API keys are now inline on the final step — one-click install creates and saves config on AIOStreams with keys baked in',
    'Manifest modal: auto-copies URL to clipboard, WuPlay/Nuvio tabs show dedicated setup instructions',
  ]},
  { v:'2.18', date:'Jul 6 2026', items:[
    'Custom Formatter import — paste or load your own formatter JSON, persists across sessions',
    'Security: auto host detection probes with lightweight GET first, no longer broadcasts credentials to all hosts',
    'Security: all external links include rel="noopener noreferrer", template name field sanitized on input',
  ]},
  { v:'2.15', date:'Jul 5 2026', items:[
    'Create & Install now races direct API calls against a Cloudflare Worker CORS proxy — most public AIOStreams hosts block cross-origin POST/PATCH, so the proxy (server-to-server, no CORS) usually wins where a direct call previously fell back to the manual paste-back flow',
    'No behavior change if a host ever adds CORS headers itself — the direct attempt still wins the race',
    'Icon refinements — TorBox Essential checkmark accent, Debrid.io magnifying glass, StreamNZB broadcast arcs, Everything 4-dot category grid, Apple TV Gen 1-2 vs Gen 3 color differentiation',
    'WebStreamr preset disabled by default — manifest endpoint returning HTML errors on multiple instances',
  ]},
  { v:'2.14', date:'Jul 4 2026', items:[
    'StreamNZB added to Other Services — self-hosted Usenet indexer + streamer, manifest URL input on API step',
    'TMDB keys now optional — collapsed by default, auto-opens if keys saved, green SET badge when configured',
    'Review step restructured — collapsible accordion sections, Install open by default, Customize collapsed',
    'Share + Import buttons side-by-side, size limit moved below template name',
    'Splash redesign — Easy Setup + presets top, free/P2P/community links under "More options" collapsible',
    'Resume banner promoted above CTAs, step dot labels enlarged for mobile',
    'Button animations calmed — hover-only effects, preset buttons use proper CSS classes',
    'Fix: requiredLanguages replaces includedLanguages on all 35 non-Anime templates',
    'Fix: Library preset removed from HTTP/P2P configs, ElfHosted excluded from P2P/HTTP install targets',
  ]},
  { v:'2.9', date:'Jul 2 2026', items:[
    'Easy Setup — new default path: pick service, device, resolution, add key, one Create &amp; Install button. Config created on public host with auto-generated password',
    'One-click deep link — template uploaded to paste.rs, AIOStreams fetches it directly. Fallback chips shown if popup blocked',
    'Dynamic addon fetching replaces addon groups — simpler config, no more "Every group must have at least one addon" errors',
    'Quick Start presets return — 4K · Lossless and 1080p · DD+ accelerators now ask for device and end on one-click install',
    'TMDB fields renamed to match AIOStreams exactly, swap detection warns on wrong-field paste',
    'Browser Back steps through wizard, step dots and receipt rows clickable for direct navigation',
    'Copy JSON button on Preview, wrong-field API key detection, audio profile ? explainers',
    'QR codes generated locally — manifest URLs no longer sent to external QR service',
    'API key test buttons — verify TorBox / RD / AllDebrid / Premiumize / Debrid-Link before generating',
    'Light theme completed — all UI elements readable in light mode',
    'Readability pass — larger text on buttons, highlighted credential warnings, mobile-friendly sizing',
    'Security: Share links contain only wizard selections (no keys/tokens/UUIDs), configs validated on load',
    'Security: Import URL strips NZBGeek / Debrid.io keys and Base Config password before upload',
    'Accessibility: screen reader announcements, reduced-motion support, focus trap in modals',
    'Fix: P2P Free auto-enables raw torrents, P2P Quick Start no longer generates TorBox presets',
    'Fix: size-limit sync, JSON preview refresh, auto host updates existing config, keyboard shortcut scoping',
  ]},
  { v:'2.2', date:'Jul 1 2026', items:[
    'Multi-select services — pick multiple debrid services, each gets its own API key field',
    'UUID smart paste — paste a full manifest URL and UUID, host, and password are auto-filled. Real-time format validation',
    'Step dots show ✓ for completed steps, Back button names the destination, Skip buttons on Device/Content',
    'Keyboard navigation — Enter to advance, Escape to go back. Receipt highlights non-defaults in cyan',
    'Share Config — encode wizard settings into a URL, copy to share. Community links on splash and Review step',
    'Formatter picker on Review step — change formatter without going back',
    'Other Services drawer — Debrid.io, P2P Free, HTTP Streams, NZBGeek in collapsible menu',
    'Stream Availability picker, Language Preferences with Exclusive mode, Nuvio install tab',
    'Resolution auto-detect — 4K pre-selected on 4K displays. Template name auto-fill on Review',
    'Responsive UX — mobile grid breakpoints, touch sizing, fresh start button',
    'Fix: duplicate API key fields, Quick Start flow, modal keyboard handling, formatter picker re-render',
  ]},
  { v:'1.6', date:'Jun 27 2026', items:[
    'Light / dark mode toggle — persists across sessions',
    'Stremio login — sign in to push template to your library. WuPlay tab auto-copies manifest URL',
    'Quick Start skips device / resolution / content steps — lands on APIs',
    'Back button on Review step, step progress dots, live changelog on splash',
    'Advanced Options redesigned — cyan border, "Audio · Video · Formatter" subtitle',
    'TMDB keys included in import URL, omitted when empty (fixes 401 errors)',
  ]},
  { v:'1.2', date:'Jun 26 2026', items:[
    'Advanced Options panel — audio profile, video prefs (4K/DV exclusions), formatter picker',
    'Match Aggressiveness — relaxed / balanced / strict deduplication modes',
    'Device → audio auto-suggestion when selecting a device profile',
    'P2P toggle on content step — enable or block uncached torrent streams',
    'Quality-first sort mode — resolution before cached status',
  ]},
  { v:'1.1', date:'Jun 25 2026', items:[
    'Quick Start presets — 4K Lossless and 1080p DD+ one-click setup',
    'Audio profiles in resolution step accordion — Lossless / DD+ / Auto / Dolby Only',
    'Install tabs in manifest modal — Stremio App · Web · WuPlay · Nuvio',
    'All steps redesigned to svc-list row layout with icons and descriptions',
  ]},
  { v:'1.0', date:'Jun 24 2026', items:[
    'Initial launch — 6-step wizard: Service → Device → Resolution → Content → APIs → Review',
    'Services: TorBox Pro / Essential, AllDebrid, Real-Debrid, Premiumize, Debrid-Link, Hybrid, Multi-Debrid, EasyNews',
    'Device profiles — Standard TV, Samsung, Apple TV, LG webOS, Windows PC, Fire Stick, Nuvio',
    'Resolution — 4K · Ultrawide · 1080p · 720p with per-resolution template routing',
    'Content type — All, Anime Only, No Anime, Live TV',
    'APIs — TorBox, AllDebrid, Real-Debrid, TMDB, instance host selection',
    'Review step — named config, one-click manifest generation and download',
    'Instance support — elfhosted, fortheweak, custom URL with UUID/password',
  ]},
];
